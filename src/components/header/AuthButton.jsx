const AuthButton = () => {
  return (
    <div className="flex flex-row justify-center align-center gap-5">
      <button>로그인</button>
      <button>회원가입</button>
    </div>
  );
};

export default AuthButton;
